package org.example.LamdaExpression;

public interface Calculator {

    //it's a interface and having abstract method
    //in lamda expression we use this method
    //lamda expression express the instance of this Calculator interface
    int calculate(int a,int b);
}
